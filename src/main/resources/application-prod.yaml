server:
  port: 8080

spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url:
    username:
    password:
    driver-class-name:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

logging:
  level:
    root: info

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

info:
  datasource:
    url: hidden